import Header from '../../../components/Header';import ShareButtons from '../../../components/ShareButtons';import {supabaseServer,publicImage} from '../../../lib/supabase';import {notFound} from 'next/navigation';export const revalidate=60;
export async function generateMetadata({params}){const {id}=await params;const {data:n}=await supabaseServer().from('news').select('headline,subheadline,content,main_image_url').eq('id',id).eq('status','published').single();if(!n)return{};const d=(n.subheadline||n.content||'').slice(0,180),image=publicImage(n.main_image_url);return{title:n.headline,description:d,openGraph:{type:'article',title:n.headline,description:d,images:image?[{url:image}]:[]},twitter:{card:'summary_large_image',title:n.headline,description:d,images:image?[image]:[]}}}
export default async function Article({params}){const {id}=await params;const {data:n}=await supabaseServer().from('news').select('id,headline,subheadline,content,location,main_image_url,published_at,created_at,categories(name)').eq('id',id).eq('status','published').single();if(!n)notFound();const image=publicImage(n.main_image_url),url=`https://metrocitynews.co.in/news/${n.id}`;return <><Header/><main className="article-wrap"><article className="article"><div className="eyebrow">{n.categories?.name||'बातमी'}</div><h1>{n.headline}</h1><div className="meta">{n.location||''}{n.location&&' • '}{n.published_at||n.created_at}</div>{image&&<img className="article-hero" src={image} alt={n.headline}/>} {n.subheadline&&<p><strong>{n.subheadline}</strong></p>}<div className="article-body">{n.content||''}</div><ShareButtons url={url} title={n.headline}/></article></main></>}
